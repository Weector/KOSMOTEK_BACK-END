const authRouter = require("./auth");
const userRouter = require("./user");
const productsRouter = require("./products");
const orderRouter = require("./order");

module.exports = { authRouter, userRouter, productsRouter, orderRouter };
